#include <bits/stdc++.h>
using namespace std;

int main()
{
  
  int num,revnum=0,rem;
  cin>>num;
  
  while(num!=0)
  {
    rem = num%10;
    revnum = revnum*10 + rem;
    num = num/10;
  }
  
  cout<<revnum;
  
  
  //write your code here
  
  return 0;
}
